---
type Props = {
  title: string;
  big: boolean;
};

const { title, big } = Astro.props;
---

<h1
  class:list={`text-text-heading mt-6 mb-4 ${big ? 'text-6xl sm:text-7xl md:text-8xl lg:text-9xl' : 'text-4xl sm:text-5xl md:text-6xl lg:text-8xl'}`}
>
  { title }
</h1>